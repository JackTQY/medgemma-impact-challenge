# MedGemma Impact Challenge — dependencies
# Install: pip install -r requirements.txt

# Orchestration & LLM
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-google-genai>=2.0.0

# Config and env
python-dotenv>=1.0.0
pyyaml>=6.0
pydantic>=2.0.0

# Optional: local MedGemma (USE_MEDGEMMA_BACKEND=local)
# pip install -r requirements-local.txt

# Web app (local demo)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
