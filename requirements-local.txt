# Optional: for USE_MEDGEMMA_BACKEND=local (run MedGemma on your machine)
# Install after base deps: pip install -r requirements.txt && pip install -r requirements-local.txt

-r requirements.txt
transformers>=4.36.0
torch>=2.0.0
accelerate>=0.25.0
