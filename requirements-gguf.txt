# For USE_MEDGEMMA_BACKEND=local_gguf (quantized MedGemma, ~1.8–2.6 GB download)
# Install after base: pip install -r requirements.txt && pip install -r requirements-gguf.txt

-r requirements.txt
llama-cpp-python>=0.2.0
langchain-community>=0.3.0
